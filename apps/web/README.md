# Angular 17 Frontend (implemented)

This app includes:
- Angular 17 application setup
- Angular Material UI controls
- Universal-style server entry (`server.ts`) for SSR hosting path
- Same-page Scan + Ask flow wired to API endpoints

Run through Nx:
```bash
nx run web:dev
```
